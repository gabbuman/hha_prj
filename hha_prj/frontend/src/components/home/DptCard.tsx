import React, { Component } from 'react';
import styled from 'styled-components'

type DptCardProps = {
    mainColor: string;
    departmentName: string;
    image: string;
}

type ColorProps = {
    mainColor: string;
}

